---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: mitmproxy-chaos- 
spec:
  template:
    metadata:
      labels:
        experiment: mitmproxy-chaos
    spec:
      # Placeholder that is updated by the executor for automated runs
      # Provide appropriate SA (with desired permissions) if executed manually
      serviceAccountName: busy
      restartPolicy: Never
      containers:
      - name: ansibletest
        image: "ansible-runner:v1"
        imagePullPolicy: Never
        env: 
          - name: ANSIBLE_STDOUT_CALLBACK
            value: 'default'

          # provide application namespace
          - name: APP_NAMESPACE
            value: 'default'

          # provide application kind, for supported see kubectl set env docu
          - name: APP_KIND
            value: 'deploy'
            
          # provide application label
          - name: APP_LABEL
            value: 'app=busy1'
            
          # provide application name
          - name: APP_NAME
            value: 'busy1'

          # provide target container (optional)
          - name: TARGET_CONTAINER
            value: ''

          # provide if custom mitmproxy script is used
          #- name: CUSTOM_SCRIPT
          #  value: 'false'

          - name: TOTAL_CHAOS_DURATION
            value: '60000' # in ms
            
          - name: LIB_IMAGE
            value: 'mitmproxy/mitmproxy:latest' 
            
          - name: RAMP_TIME
            value: ''

           # provide chaosengine name
          - name: CHAOSENGINE
            value: ''

          - name: CHAOS_SERVICE_ACCOUNT
            valueFrom:
              fieldRef:
                fieldPath: spec.serviceAccountName

        command: ["/bin/bash"]
        args: ["-c", "ansible-playbook ./experiments/generic/mitmproxy_chaos/mitmproxy_chaos_ansible_logic.yml -i /etc/ansible/hosts -vv; exit 0"]
        
